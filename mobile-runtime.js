let mView='recommend',mModule=null,mExpanded=new Set(),mSearch='';const mr=()=>document.querySelector('#mobileRoot');function mIsMobile(){return matchMedia('(max-width:768px)').matches}function mDay(p,d,i){if(!d?.daily||i>=d.daily.time.length)return'';const f=fmtDate(d.daily.time[i]),s=scoreDay(d.daily.weather_code[i],d.daily.precipitation_probability_max[i],d.daily.precipitation_sum[i],d.daily.temperature_2m_max[i],p.sensitivity);return `<div class="m-day ${cls(s)}"><div class="m-date">${i===0?'今天':f.day+' '+f.dow}</div><div class="m-icon">${iconFor(d.daily.weather_code[i])}</div><div class="m-temp">${Math.round(d.daily.temperature_2m_max[i])}° / ${Math.round(d.daily.temperature_2m_min[i])}°</div><div class="m-rain">💧 ${d.daily.precipitation_probability_max[i]??0}% · ${Number(d.daily.precipitation_sum[i]||0).toFixed(1)}mm</div></div>`}function mCard(p,rank){const d=weatherCache[p.id],score=d?.recScore??'—',dist=d?.distanceKm!=null?' · '+d.distanceKm+' km':'';let days='<div class="m-days">';if(!d)days+='<div class="m-empty" style="grid-column:1/-1;padding:20px">天气加载中…</div>';else if(d.error)days+=`<div class="m-empty" style="grid-column:1/-1;padding:20px">⚠️ ${d.error}</div>`;else for(let i=0;i<Math.min(3,d.daily.time.length);i++)days+=mDay(p,d,i);days+='</div>';let more='';if(mExpanded.has(p.id)&&d?.daily){more='<div class="m-scroll">';for(let i=0;i<Math.min(14,d.daily.time.length);i++)more+=mDay(p,d,i);more+='</div>'}return `<div class="m-card"><div class="m-card-top"><div><div class="m-name">${rank?`<span class="m-rank">${rank}</span>`:''}${p.name}</div><div class="m-meta">${p.module||'未分类'} · 预计 ${p.stay||'—'}${dist}</div></div><div class="m-score"><strong>${score}</strong><span>推荐分</span></div></div>${days}<div class="m-actions"><button class="m-btn" data-x="${p.id}">${mExpanded.has(p.id)?'收起':'查看14天'}</button><button class="m-btn" data-f="${p.id}">${favorites.has(p.id)?'★ 已收藏':'☆ 收藏'}</button><button class="m-btn" data-e="${p.id}">编辑</button></div>${more}</div>`}function mRecommend(){const ranked=places.filter(p=>weatherCache[p.id]?.recScore!=null).sort((a,b)=>weatherCache[b.id].recScore-weatherCache[a.id].recScore).slice(0,3);let h='<div class="m-section">接下来去哪？</div><div class="m-note">综合天气、预计停留时间和当前位置距离。</div>';if(!ranked.length)h+='<div class="m-empty">正在获取天气…</div>';else ranked.forEach((p,i)=>h+=mCard(p,i+1));return h}function mAll(){const q=mSearch.toLowerCase(),arr=places.filter(p=>!q||(p.name+' '+p.query+' '+(p.module||'')).toLowerCase().includes(q));let h=`<div class="m-section">全部目的地</div><input class="m-search" id="mSearch" placeholder="搜索目的地…" value="${mSearch.replace(/"/g,'&quot;')}">`;arr.forEach(p=>h+=mCard(p));return h||'<div class="m-empty">没有目的地</div>'}function mModules(){let h='<div class="m-section">模块</div><div class="m-note">按你的攻略结构查看相近地点。</div>';modules().forEach(name=>{const ps=places.filter(p=>(p.module||'未分类')===name),s=moduleScore(name);h+=`<div class="m-module"><div class="m-module-top"><div><div class="m-name">${name}</div><div class="m-meta">${ps.length} 个目的地</div></div><div class="m-module-score">${s??'—'}</div></div><div class="m-module-list">${ps.length?ps.map(p=>p.name).join(' · '):'暂无目的地'}</div><div class="m-actions"><button class="m-btn m-primary" data-m="${encodeURIComponent(name)}">进入模块</button></div></div>`});return h}function mModuleDetail(){const ps=places.filter(p=>(p.module||'未分类')===mModule).sort((a,b)=>(weatherCache[b.id]?.recScore??-1)-(weatherCache[a.id]?.recScore??-1));let h=`<button class="m-btn" id="mBack">← 所有模块</button><div class="m-section">${mModule}</div><div class="m-note">${ps.length} 个目的地。</div>`;ps.forEach(p=>h+=mCard(p));return h}function mRender(){if(!mIsMobile()||!mr())return;const loc=currentLocation?(currentLocation.name||'设备当前位置'):'尚未设置';mr().innerHTML=`<div class="m-head"><div class="m-title-row"><div><div class="m-eyebrow">WEATHER-FIRST TRAVEL PLANNER</div><div class="m-title">Italy Trip 🇮🇹</div><div class="m-sub">根据未来天气决定接下来 2–3 天去哪。</div></div><button class="m-refresh" id="mRefresh">↻</button></div><div class="m-location"><div><div class="m-label">当前位置</div><div class="m-location-name">${loc}</div></div><button class="m-btn" id="mLoc">设置位置</button></div></div><div class="m-content">${mView==='recommend'?mRecommend():mView==='all'?mAll():mView==='modules'?mModules():mModuleDetail()}</div><div class="m-tabs"><button data-v="recommend" class="${mView==='recommend'?'active':''}"><span class="m-tab-icon">✦</span>推荐</button><button data-v="all" class="${mView==='all'?'active':''}"><span class="m-tab-icon">☰</span>全部目的地</button><button data-v="modules" class="${mView==='modules'||mView==='module'?'active':''}"><span class="m-tab-icon">▦</span>模块</button></div>`;mBind()}function mBind(){document.querySelectorAll('[data-v]').forEach(b=>b.onclick=()=>{mView=b.dataset.v;mModule=null;mRender()});document.querySelectorAll('[data-x]').forEach(b=>b.onclick=()=>{mExpanded.has(b.dataset.x)?mExpanded.delete(b.dataset.x):mExpanded.add(b.dataset.x);mRender()});document.querySelectorAll('[data-f]').forEach(b=>b.onclick=()=>{const id=b.dataset.f;favorites.has(id)?favorites.delete(id):favorites.add(id);localStorage.setItem('italyTripFavorites',JSON.stringify([...favorites]));mRender()});document.querySelectorAll('[data-e]').forEach(b=>b.onclick=()=>openEdit(b.dataset.e));document.querySelectorAll('[data-m]').forEach(b=>b.onclick=()=>{mModule=decodeURIComponent(b.dataset.m);mView='module';mRender()});const back=document.querySelector('#mBack');if(back)back.onclick=()=>{mView='modules';mModule=null;mRender()};const search=document.querySelector('#mSearch');if(search)search.oninput=e=>{mSearch=e.target.value;mRender()};const ref=document.querySelector('#mRefresh');if(ref)ref.onclick=loadAll;const loc=document.querySelector('#mLoc');if(loc)loc.onclick=()=>mLocationSheet()}function mLocationSheet(){const overlay=document.createElement('div');overlay.className='m-modal';overlay.innerHTML=`<div class="m-sheet"><h3>设置当前位置</h3><button class="m-btn m-primary" id="mAuto" style="width:100%;margin-bottom:10px">⌖ 自动获取当前位置</button><div class="m-label">或手动输入城市</div><input id="mCity" placeholder="Rome / Naples / Florence"><div class="m-actions"><button class="m-btn" id="mCancel">取消</button><button class="m-btn m-primary" id="mSet">设置</button></div></div>`;document.body.appendChild(overlay);overlay.onclick=e=>{if(e.target===overlay)overlay.remove()};overlay.querySelector('#mCancel').onclick=()=>overlay.remove();overlay.querySelector('#mAuto').onclick=()=>{overlay.remove();document.querySelector('#autoLocationBtn').click()};overlay.querySelector('#mSet').onclick=()=>{const v=overlay.querySelector('#mCity').value.trim();if(!v)return;document.querySelector('#manualLocation').value=v;overlay.remove();document.querySelector('#manualLocationBtn').click()}}const oldRenderTable=renderTable;renderTable=function(){oldRenderTable();mRender()};const oldRenderModules=renderModules;renderModules=function(){oldRenderModules();mRender()};const oldUpdateLocationUI=updateLocationUI;updateLocationUI=function(){oldUpdateLocationUI();mRender()};window.addEventListener('resize',mRender);setTimeout(mRender,0);